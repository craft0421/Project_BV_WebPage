<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title>메인 페이지</title>
</head>
<body>
  <% if (user) { %>
    <h2>안녕하세요, <%= user.name %>님!</h2>
    <p>전화번호: <%= user.phone %></p>
    <p>권한: <%= user.role === 1 ? '관리자' : '일반 사용자' %></p>
    <form action="/logout" method="post">
      <button type="submit">로그아웃</button>
    </form>
  <% } else { %>
    <h2>환영합니다!</h2>
    <p>아직 로그인하지 않으셨습니다.</p>
    <a href="/login">로그인</a>
    <a href="/signup">회원가입</a>
  <% } %>
</body>
</html>